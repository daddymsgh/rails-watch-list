<section class="cinemabg py-5 text-center">
  <div class="container">
    <div class="row py-lg-5">
      <div class="col-lg-6 col-md-8 mx-auto">
        <h1 class="fw-light text-white"><%= @list.name %></h1>
        <p class="lead text-white">Your <%= @list.name %> watchlist</p>
        <p>
          <p>
<%= link_to new_list_bookmark_path(@list), class: 'btn btn-primary my-2' do %>
  Add a Movie
<% end %>
        </p>
      </div>
    </div>
    </div>
  </section>

 <div class="container">
    <h2 class="mt-3 mb-3">All my <%= @list.name %></h2>
    <div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 g-3">
    <% @list.bookmarks.each do |bookmark| %>
      <div class="col">
      <div class="card shadow-sm">
            <svg class="bd-placeholder-img card-img-top" width="100%" height="225" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="Placeholder: Thumbnail" preserveAspectRatio="xMidYMid slice" focusable="false"><title>Placeholder</title><rect width="100%" height="100%" fill="#55595c"></rect><text x="50%" y="50%" fill="#eceeef" dy=".3em"><%= bookmark.comment %></text></svg>
            <div class="card-body">
              <p class="card-text"><%= bookmark.comment %></p>
              <div class="d-flex justify-content-between align-items-center">
                <div class="btn-group">
                                  <button type="button" class="btn btn-sm btn-outline-secondary">Edit</button>
                  <%= link_to 'Delete', bookmark_path(bookmark), data: { turbo_method: :delete, turbo_confirm: 'Are you sure?' }, class: 'btn btn-sm btn-outline-secondary'  %>
            </li>
                </div>
                <small class="text-body-secondary">12h 41min</small>
              </div>
            </div>
          </div>
          </div>
    <% end %>
    </div>
</div>
